# ── LangChain core ────────────────────────────────────────────────
langchain>=1.3.0,<1.4.0
langchain-core>=1.6.0,<1.7.0
langchain-community>=0.4.0,<0.5.0
langchain-text-splitters>=1.1.0,<1.2.0

# ── LLM Providers (cài tất cả để hỗ trợ đa provider) ─────────────
langchain-openai>=1.6.0,<1.7.0   # OpenAI + OpenRouter
langchain-anthropic>=1.6.0,<1.7.0 # Anthropic Claude
langchain-google-genai>=4.3.0,<4.4.0 # Google Gemini
langchain-ollama>=1.1.0,<1.2.0   # Ollama (local)

# ── LangSmith ─────────────────────────────────────────────────────
langsmith>=0.2.0

# ── Vector store ──────────────────────────────────────────────────
faiss-cpu>=1.7.0

# ── RAG Evaluation ────────────────────────────────────────────────
ragas>=0.4.0
datasets>=2.0.0                  # required by RAGAS internally
numpy>=1.25.0
transformers>=4.57.0,<5.0.0
torch>=2.13.0                     # required by the local HHEM evaluator

# ── Guardrails AI ─────────────────────────────────────────────────
guardrails-ai>=0.11.0,<0.12.0

# ── Utilities ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
openai>=2.54.0,<3.0.0             # OpenAI SDK (used by langchain-openai)
tiktoken>=0.5.0                  # token counting
Pillow>=10.0.0                   # render the RAGAS evidence image
